# Makefile
# This is a makefile that conveniently calls Phoenix's makefile when Phoenix is a dependency
# Should be copied to project's root directory

PHOENIX_DIR=$(wildcard Packages/Phoenix-*)

make:
	make -f ${PHOENIX_DIR}/Makefile
